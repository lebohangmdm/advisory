import { LucideIcon } from "lucide-react";

export type Link = {
  id: string;
  href: string;
  label: string;
};

export type serviceData = {
  title: string;
  description: string;
  icon: LucideIcon;
};
